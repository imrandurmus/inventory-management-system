import React from "react";

const DashboardEmployee: React.FC = () => {
    return
    (
        <div className="dashboard employee">
            <h2>Welocme to the Employee dashboard</h2>
            <p>You're logged in as an employee'.</p>
        </div>
    );
};

export default DashboardEmployee;
